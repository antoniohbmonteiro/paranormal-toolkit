import {expect,it} from "vitest";import {readFileSync,readdirSync} from "node:fs";import {join} from "node:path";const files=(dir:string)=>readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?files(join(dir,e.name)):[join(dir,e.name)]);it("mantém fronteiras dos componentes e exemplos",()=>{const components=files("src/ui/components").map(readFileSyncText).join("\n");const examples=files("src/ui/examples").map(readFileSyncText).join("\n");expect(components).not.toMatch(/from ["'][^"']*features|from ["'][^"']*examples|\b(game|Hooks|ChatMessage|Actor|Item|Roll)\b/);expect(examples).not.toMatch(/from ["'][^"']*features/);expect(examples).not.toContain("diceBreakdown");expect(examples).not.toMatch(/binder|controller/i);const features=files("src/features").map(readFileSyncText).join("\n");expect(features).not.toMatch(/from ["'][^"']*ui\/examples/)});function readFileSyncText(path:string){return readFileSync(path,"utf8")}
