import { expect, it } from "vitest";import { readFileSync,readdirSync } from "node:fs";import { join } from "node:path";const files=(dir:string):string[]=>readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?files(join(dir,e.name)):[join(dir,e.name)]);it("mantém componentes puros e controller local sem documentos",()=>{const components=files("src/ui/components").map(read).join("\n");expect(components).not.toMatch(/from ["'][^"']*features|\b(game|Hooks|ChatMessage|Actor|Item|Roll)\b/u);const controller=read("src/ui/examples/ritual-preview-controller.ts");expect(controller).not.toMatch(/\b(game|Hooks|ChatMessage|Actor|Item|Roll|workflow)\b/u);const examples=files("src/ui/examples").map(read).join("\n");expect(examples).not.toMatch(/from ["'][^"']*features/u)});function read(path:string){return readFileSync(path,"utf8")}
