import { expect,it } from "vitest";import { renderChatCardHeader } from "../../../../src/ui/components";
it("renderiza imagem, fallback, textos e badges com escape",()=>{const html=renderChatCardHeader({image:{src:null,fallbackSrc:"a&b",alt:'<alt>'},eyebrow:"<tipo>",title:"Título muito longo & seguro",badges:[{label:'"um"',tone:"info"},{label:"dois"}]});expect(html).toContain('src="a&amp;b"');expect(html).toContain("&lt;tipo&gt;");expect(html).not.toContain("__subtitle");expect(html.match(/__badge--/g)).toHaveLength(2)});
