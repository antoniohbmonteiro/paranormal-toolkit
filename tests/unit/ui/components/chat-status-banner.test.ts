import {expect,it} from "vitest";import {renderChatStatusBanner} from "../../../../src/ui/components";it("renderiza todos os tons sem APIs interativas",()=>{for(const tone of ["success","failure","info","resource","warning","manual"] as const){const html=renderChatStatusBanner({tone,message:"<m>",title:tone==="success"?"T":null});expect(html).toContain(`--${tone}`);expect(html).toContain("&lt;m&gt;");expect(html).not.toContain("aria-live");expect(html).not.toContain("icon")}});
