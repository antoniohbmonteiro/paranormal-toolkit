import {expect,it} from "vitest";import {componentHeaderExamples,componentRollExamples,componentSectionExamples,componentStatusExamples} from "../../../../src/ui/examples/component-example-fixtures";it("cobre catálogos estáticos",()=>{expect(componentHeaderExamples.length).toBeGreaterThanOrEqual(5);expect(componentSectionExamples.map(x=>x.tone)).toEqual(["generic","test","damage","healing","resistance"]);expect(componentRollExamples.some(x=>x.dice?.some(d=>d.state==="discarded"))).toBe(true);expect(new Set(componentStatusExamples.map(x=>x.tone)).size).toBe(6)});
