import { expect, it } from "vitest";
import { RitualPreviewController } from "../../../../src/ui/examples/ritual-preview-controller";
it("altera apenas o estado local do preview",()=>{const controller=new RitualPreviewController();expect(controller.snapshot().damage.resistance.state).toBe("pending");controller.selectConjurationFormula("focused");expect(controller.snapshot().conjuration.status).toBe("failure");controller.selectDamageFormula("amplified");expect(controller.snapshot().damage.selectedFormulaId).toBe("amplified");controller.resolveResistance("success");expect(controller.snapshot().damage.resistance.total).toBe(25);controller.applyDamage();expect(controller.snapshot().damage.damageApplied).toBe(true);controller.resolveEffectResistance("failure");expect(controller.snapshot().effect.resistanceState).toBe("failure")});
