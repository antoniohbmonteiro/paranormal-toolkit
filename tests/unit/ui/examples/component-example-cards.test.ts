import { expect, it } from "vitest";
import { renderComponentExampleCards } from "../../../../src/ui/examples/component-example-cards";
it("publica somente o ritual single-target completo e seguro",()=>{const cards=renderComponentExampleCards(true);expect(cards).toHaveLength(1);expect(cards[0]?.kind).toBe("ritual-single-target");const html=cards[0]!.html;for(const region of ["ritual-context","ritual-workflow--conjuration","ritual-workflow--damage","ritual-resistance","ritual-assisted-action","ritual-effect"])expect(html).toContain(region);expect(html).not.toMatch(/Habilidade fictícia|Equipamento fictício|Teste de largura|stress/iu);expect(html).not.toMatch(/<button(?![^>]*disabled)/u)});
