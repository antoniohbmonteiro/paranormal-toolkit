import {expect,it} from "vitest";import {renderComponentExampleCards} from "../../../../src/ui/examples/component-example-cards";it("compõe quatro cards fictícios e desabilita ações no chat",()=>{const cards=renderComponentExampleCards(true);expect(cards.map(x=>x.kind)).toEqual(["ability","ritual","weapon","stress"]);for(const card of cards){expect(card.html).toContain("paranormal-toolkit-chat-card-header");expect(card.html).toContain("paranormal-toolkit-chat-card-metadata");expect(card.html).not.toMatch(/<button(?![^>]*disabled)/)}expect(cards.find(card=>card.kind==="stress")?.html).toContain("paranormal-toolkit-chat-card--narrow")});
