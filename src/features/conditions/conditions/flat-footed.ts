import { defineInformativeCondition } from "../condition-definition";

export const FLAT_FOOTED_CONDITION = defineInformativeCondition({
  id: "flatFooted",
  label: "Desprevenido",
  aliases: ["desprevenido"]
});
