import { defineInformativeCondition } from "../condition-definition";

export const FATIGUED_CONDITION = defineInformativeCondition({
  id: "fatigued",
  label: "Fatigado",
  aliases: ["fatigado"]
});
